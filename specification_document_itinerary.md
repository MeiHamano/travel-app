# 【旅行旅程表作成アプリ】仕様設計ドキュメント

## 1. アプリの目的とスコープ
* **解決したい課題/目的:** 
  * 複数日（1泊2日、2泊3日など）の旅行スケジュールを1本のタイムラインで管理しつつ、**日付の区切り（1日目、2日目…）を明確にして**見やすい旅程表を作成したい。
  * 「移動」や「ご飯」といったカテゴリに応じて必要な入力項目を自動で出し分け、迷わず入力・閲覧できるようにする。
* **想定ユーザー:** 
  * 宿泊を伴う旅行や複数日のお出かけスケジュールを整理したいユーザー
* **やること (In Scope):**
  * 日付区切りブロック（ディバイダー）の挿入・編集・削除
  * タイムライン形式（時間枠）でのスケジュール項目の追加・編集・削除
  * 開始時間・終了時間の入力/選択
  * カテゴリ選択（移動 / ご飯 / アクティビティ / ショッピング / その他）
  * カテゴリごとの入力フォームの動的切り替え
    * 「移動」選択時：移動手段セレクトボックス、URL入力、メモ入力
    * 「移動手段：その他」選択時：自由記述エディター（テキスト入力）の表示
    * 「ご飯 / アクティビティ / ショッピング / その他」選択時：目的の場所、URL、メモ入力
* **やらないこと (Out of Scope):**
  * ユーザー認証 (ログイン機能)
  * バックエンドAPIとの通信
  * チーム共有機能

---

## 2. 機能一覧と仕様
| 機能名 | 概要 | 完了条件 (受け入れ基準) |
|---|---|---|
| **日付区切りの挿入** | 「1日目（日付）」「2日目（日付）」といった区切りブロックを挿入 | リスト最上部に初期配置され、下部アクションから「次の日付を追加」で新しい日を区切るブロックを追加できること |
| **旅程アイテム追加** | スケジュール枠をタイムラインに追加 | 「＋ 予定を追加」ボタンで、タイムラインに新しい予定枠が挿入されること |
| **時間設定** | 左側に開始時間・終了時間を指定（例: `10:00` 〜 `11:00`） | 開始時刻・終了時刻が正しく保持されること |
| **カテゴリ選択** | ドロップダウンから活動種別を選択 | 「移動」「ご飯」「アクティビティ」「ショッピング」「その他」から選択でき、入力枠が動的に切り替わること |
| **移動手段の分岐** | カテゴリが「移動」の場合の専用入力 | 候補（徒歩/電車/バス/車/飛行機/その他）から選択可能。「その他」選択時のみ自由記述テキスト枠が出現すること |
| **URL・メモの入力** | 各カテゴリに応じた詳細情報の入力 | URL（別タブリンク）およびメモが正しく保持・表示されること |
| **削除機能** | 日付区切り枠、または個別予定枠の削除 | ゴミ箱アイコン押下で、該当のブロックがタイムラインから即時除外されること |

---

## 3. 画面・状態 (State) 設計
### 3.1 画面構成 / UIレイアウト
1本のタイムラインの中に、「日付区切りブロック」と「予定ブロック」が並ぶ構成。

* **ヘッダー**: アプリタイトル（「タイムスケジュール」）
* **メインエリア（タイムライン一覧）**:
  * **【日付区切りブロック】**（最初および各日のはじまり）:
    * `1日目: [ 日付選択 ]` ＋ 削除ボタン
  * **【予定ブロック（カード/Paper）】**:
    * **左側**: 時間入力部（開始時刻 `[10:00]` 〜 終了時刻 `[11:00]`）
    * **中央**: カテゴリ選択ドロップダウン
    * **右側（動的フォーム部）**:
      * **【移動】の場合:** 移動手段セレクト（その他時は自由入力）、URL、メモ
      * **【ご飯/アクティビティ等】の場合:** 目的の場所、URL、メモ
    * **操作部**: 削除ボタン
* **フッター/下部アクション**:
  * **「＋ 予定を追加」ボタン**（スケジュール行を増やす）
  * **「＋ 日付（区切り）を追加」ボタン**（2日目、3日目の仕切りブロックを挿入する）

### 3.2 考慮すべき状態一覧
* [x] **通常時 (Default)**: 日付区切りと登録済みのスケジュールが時系列で見やすく並んでいる状態
* [x] **カテゴリ切り替え時**: カテゴリを変更した際、対応する入力フィールド（移動手段 or 目的の場所）がスムーズに切り替わること
* [x] **データが0件の時 (Empty State)**: 予定が空の場合でも、追加ボタンからスケジュール作成を開始できる状態
* [x] **入力バリデーション**: 時間の順序（終了時刻が開始時刻より前になっていないかのチェック考慮）

### 3.3 コンポーネント分割とStateの配置
* **`App.jsx` (親コンポーネント)**
  * **保持するState:**
    * `timelineList`: `Array<TimelineItem>`（日付区切りと旅程予定が時系列に並んだ一覧） 

* **子コンポーネント構成:**
  * **`<DateDividerRow/>`**: 日付区切りバー表示行
    * 「○日目」のバッジ表示と日付選択カレンダー、区切り削除ボタン
  * **`<ItineraryItemRow/>`**: 1行分のタイムライン入力・表示行
    * 左側（時間入力部）、中央（カテゴリ選択）、削除ボタンを管理
    * カテゴリの選択状態に応じて `<MoveForm/>` または `<GeneralForm/>` を動的に呼び出す
  * **`<MoveForm/>`**: 移動選択時の専用フォーム
    * 移動手段セレクトボックス（徒歩 / 電車 / バス / 車 / 飛行機 / その他）
    * ※「その他」選択時のみ表示される自由記述テキストエディター
    * URL入力エディター、メモ入力エディター
  * **`<GeneralForm/>`**: ご飯 / アクティビティ / ショッピング / その他選択時のフォーム
    * 「目的の場所・スポット名」入力エディター
    * URL入力エディター、メモ入力エディター

---

## 4. カテゴリ別フォーム表示ルール
| 選択カテゴリ | 出現する入力フィールド |
|---|---|
| **移動 (`move`)** | ① 移動手段セレクト（※「その他」時は自由入力枠）<br>② 参考/予約URL<br>③ メモ |
| **ご飯 (`meal`)** | ① 目的の場所（店名など）<br>② 参考/予約URL<br>③ メモ |
| **アクティビティ (`activity`)** | ① 目的の場所（施設名・体験名など）<br>② 参考/予約URL<br>③ メモ |
| **ショッピング (`shopping`)** | ① 目的の場所（モール・店名など）<br>② 参考/予約URL<br>③ メモ |
| **その他 (`other`)** | ① 目的・内容<br>② 参考/予約URL<br>③ メモ |

---

## 5. 実装タスク分解 (TODO)
* [ ] プロジェクト環境の準備（MUI コンポーネントの選定）
* [ ] データ型定義（日付区切り用・予定用の共用体型 `TimelineItem`）
* [ ] 初期状態（1日目区切り ＋ 空の予定1件）の作成
* [ ] フッターのアクション実装（「予定追加」と「日付追加」の2つのボタン）
* [ ] 日付区切りコンポーネント（`<DateDividerRow/>`）のUI実装
* [ ] 予定行コンポーネント（`<ItineraryItemRow/>`）のUI構築
* [ ] `<MoveForm/>` および `<GeneralForm/>` の動的切り替えと入力制御
* [ ] 移動手段が「その他」の際のテキストエディター表示切り替え制御
