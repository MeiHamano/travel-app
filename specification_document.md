# 【機能/アプリ名】仕様設計ドキュメント

## 1. アプリの目的とスコープ
* **解決したい課題/目的:** (例: 日常のタスクを忘れずに管理し、完了率を可視化したい)
* **想定ユーザー:** (例: 簡易的にタスクを管理したい個人)
* **やること (In Scope):**
  * タスクの作成・編集・削除
  * 完了ステータスの切り替え
  * ローカルストレージへの永続化
* **やらないこと (Out of Scope):**
  * ユーザー認証 (ログイン機能)
  * バックエンドAPIとの通信
  * チーム共有機能

---

## 2. 機能一覧と仕様
| 機能名 | 概要 | 完了条件 (受け入れ基準) |
|---|---|---|
| タスク追加 | タイトルを入力してEnter/ボタン押下で追加 | 空文字の場合は追加不可、一覧の先頭に即時反映されること |
| ステータス変更 | チェックボックスで完了/未完了を切替 | 完了時は打ち消し線を表示し、状態が維持されること |
| タスク削除 | 削除ボタン押下で一覧から除外 | 確認ダイアログなしで即座に削除されること |

---

## 3. 画面・状態 (State) 設計
### 3.1 画面構成 / ワイヤーフレーム
(※ 手書きスケッチや Excalidraw、FigmaなどのURL/画像を添付)

### 3.2 考慮すべき状態一覧
* [ ] **通常時 (Default / Success)**
* [ ] **データが0件の時 (Empty State)**
* [ ] **入力エラー時 (バリデーション違反)**
* [ ] **ローディング時 (必要な場合)**

### 3.3 コンポーネント分割とStateの配置
* **App (親コンポーネント)**
  * 保持するState: `tasks` (タスク一覧データ), `filter` (表示絞り込み条件)
  * 子コンポーネント:
    * `<TaskInput />`: 新規入力フォーム (内部State: `inputValue`)
    * `<TaskList />`: タスク一覧のレンダリング
      * `<TaskItem />`: 個別タスク行 (完了トグル・削除ボタン)
    * `<TaskFilter />`: 全て/未完了/完了の切り替えタブ

---

## 4. 実装タスク分解 (TODO)
* [ ] プロジェクト初期セットアップ (Vite + React + TailwindCSS等)
* [ ] コンポーネントの骨組み (静的UI) 作成
* [ ] State・ロジックの実装 (追加・削除・完了トグル)
* [ ] ローカルストレージ連携
* [ ] バリデーション・Empty画面の実装
* [ ] セルフチェック・動作確認
