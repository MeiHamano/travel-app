# 【旅行準備TODOアプリ】仕様設計ドキュメント

## 1. アプリの目的とスコープ
* **解決したい課題/目的:** 
  * 旅行の準備において「毎回必要な基本の持ち物」を一からリストアップする手間や忘れ物を減らす。
  * 泊数や旅行先（国内・海外）に合わせて、必要な個数や持ち物を自動で最適化してパッキングを効率化する。
* **想定ユーザー:** 
  * 国内外の旅行・出張を手軽に準備したい人
* **やること (In Scope):**
  * 旅行条件（国内/海外、泊数）の選択・入力
  * 条件に基づくデフォルト持ち物リストの自動生成（泊数に応じた必要数の自動計算、海外専用アイテムの出し分け）
  * 泊数や行先を変更しても足した持ち物が消えないようにする処理
  * タスクの新規作成・削除
  * 完了ステータスの切り替え
  * 残りタスク件数のリアルタイム表示
* **やらないこと (Out of Scope):**
  *  ユーザー認証 (ログイン機能)
  * バックエンドAPIとの通信
  * チーム共有機能

---

## 2. 機能一覧と仕様
| 機能名 | 概要 | 完了条件 (受け入れ基準) |
|---|---|---|
| **旅行先切替** | 国内 / 海外 のラジオボタン選択 | 海外選択時のみ「パスポート」等の海外専用アイテムがリストに追加されること。国内へ戻すと自動で除外されること |
| **泊数指定** | 宿泊日数を数値入力 | 泊数に応じてシャンプー・着替え等の日別アイテムの数量（例: `3着` / `3回分`）が自動再計算されること |
| **デフォルト項目生成** | 条件に応じた基本荷物の自動展開 | 条件変更時に手動で追加されたカスタムタスクを消去せず維持すること |
| **タスク追加** | 自由な持ち物を入力して追加 | 空白・スペースのみは追加不可。Enterキーまたは追加ボタン押下で一覧に追加されること |
| **ステータス変更** | チェックボックスで完了/未完了を切替 | 完了時はテキストに打ち消し線が適用され、残りタスク数が減ること |
| **タスク削除** | ゴミ箱アイコン押下で削除 | 該当タスクが一覧から即時除外され、残りタスク数が正しく更新されること |

---

## 3. 画面・状態 (State) 設計
### 3.1 画面構成
1. **ヘッダー**: アプリタイトル（「🧳 旅行準備TODOリスト」）
2. **条件設定エリア (Paper)**:
   * 旅行先選択ラジオボタン（国内 / 海外）
   * 宿泊数入力フィールド（`○ 泊 ○+1 日` の表示）
3. **新規タスク入力エリア (`TodoInput`)**:
   * 入力フォーム（TextField）＋ 追加ボタン（Button）
4. **ステータス表示**:
   * 「残りタスク: ○ 件」
5. **タスク一覧エリア (`List` / `TodoItem`)**:
   * チェックボックス、持ち物名＋数量表記、削除ボタン

### 3.2 考慮すべき状態一覧
* [x] **通常時 (Default / Success)**: デフォルト生成アイテムおよび手動追加アイテムが一覧表示されている状態
* [x] **条件変更時**: 泊数や国内外の変更に合わせてデフォルト項目のみが動的に書き換わり、手動追加アイテムが保持されている状態
* [x] **データが0件の時**: 全て削除された場合でも、新規追加フォームや条件設定が正常に操作可能な状態
* [x] **入力バリデーション**: 空白文字入力の防止、泊数の下限（1以上）

### 3.3 コンポーネント分割とStateの配置
* **`App.jsx` (親コンポーネント)**
  * **保持するState:**
    * `isOverseas`: `boolean`（国内: `false`, 海外: `true`）
    * `nights`: `number`（宿泊数、初期値: `2`）
    * `text`: `string`（新規タスク入力中の文字列）
    * `todos`: `Array<Todo>`（タスク一覧配列）
    
  * **子コンポーネント:**
    * `<TodoInput/>`: 新規入力フォーム（Props: `text`, `setText`, `onAdd`）
    * `<TodoItem/>`: 個別タスク行（Props: `todo`, `onToggle`, `onDelete`）

---

## 4. 持ち物定義マスター (`DEFAULT_ITEMS`)
| キー (`key`) | アイテム名 (`name`) | 単位 (`unit`) | 種別 (`type`) | 算出ロジック |
|---|---|---|---|---|
| `shampoo` | シャンプー | 回分 | `daily` | 宿泊数と同数（最低1） |
| `clothes` | 着替え | 着 | `daily` | 宿泊数と同数（最低1） |
| `lotion` | 化粧水 | 本 | `fixed` | 固定（1本） |
| `makeup` | メイク道具 | 式 | `fixed` | 固定（1式） |
| `charger` | 充電器 | 個 | `fixed` | 固定（1個） |
| `passport` | パスポート | 冊 | `overseas` | 海外旅行時のみ追加（固定1冊） |

---

## 5. 実装タスク分解 (TODO)
* [x] プロジェクト構成の確認（React + Vite + MUI）
* [x] 持ち物マスターデータ定義の作成
* [x] `App.jsx` に旅行条件用Stateの追加
* [x] 条件を変えても、自分で足した持ち物を残したまま基本セットを自動更新する仕組みの実装
* [x] MUI（`Paper`, `RadioGroup`, `TextField`）を用いた設定パネルのUI構築
